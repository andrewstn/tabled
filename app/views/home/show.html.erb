<% content_for :title, "Your table · Tabled" %>

<section class="paper-panel max-w-2xl p-8 sm:p-12">
  <p class="eyebrow">Your workspace</p>
  <h1 class="mt-3 text-4xl font-semibold tracking-tight text-ink">Welcome, <%= current_user.name %>.</h1>
  <p class="mt-4 max-w-xl text-lg leading-8 text-ink-soft">This is where your organizations gather and get the semester organized.</p>
  <div class="mt-8">
    <%= link_to "Start an organization", new_organization_path, class: "button-primary inline-block" %>
  </div>
</section>
